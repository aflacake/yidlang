#include "ExecutionGate.hpp"

ProgramMatsev ExecutionGate::evaluate(
    const StaticLegitimacy& legitimacy,
    const DecisionLedger& ledger
) const {
    // stub: selalu mengizinkan
    return ProgramMatsev::muter;
}